#!include:.gitignore

# Vector data now lives in PostgreSQL (Cloud SQL), not a local directory
# baked into the image, so nothing needs to be force-included here anymore
# -- the default fallback to .gitignore's rules is correct as-is.
